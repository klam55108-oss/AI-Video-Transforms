# Environment Variables for CognivAgent
# Copy this file to .env and fill in your actual API keys
# IMPORTANT: Never commit .env to version control!

# =============================================================================
# REQUIRED: OpenAI API Key
# =============================================================================
# Used for: Whisper transcription (speech-to-text)
# Get your key at: https://platform.openai.com/api-keys
OPENAI_API_KEY=sk-your-openai-api-key-here

# =============================================================================
# REQUIRED: Anthropic API Key
# =============================================================================
# Used for: Claude Agent SDK (the AI agent that uses the tools)
# Get your key at: https://console.anthropic.com/settings/keys
ANTHROPIC_API_KEY=sk-ant-your-anthropic-api-key-here

# =============================================================================
# OPTIONAL: Application Configuration
# =============================================================================
# All settings have sensible defaults. Override only if needed.
# Prefix: APP_ (automatically stripped by pydantic-settings)

# --- Claude Model Configuration ---
# Model ID for Claude Agent SDK
# APP_CLAUDE_MODEL=claude-opus-4-5

# Maximum concurrent Claude API calls (prevents cost blowouts)
# APP_CLAUDE_API_MAX_CONCURRENT=2

# --- Timeout Configuration (seconds) ---
# Maximum time to wait for agent response
# APP_RESPONSE_TIMEOUT=300.0

# Timeout for initial greeting message
# APP_GREETING_TIMEOUT=30.0

# Session time-to-live before auto-cleanup
# APP_SESSION_TTL=3600.0

# Interval between cleanup task runs
# APP_CLEANUP_INTERVAL=300.0

# Grace period for shutdown to complete pending work
# APP_GRACEFUL_SHUTDOWN_TIMEOUT=5.0

# --- Queue/Cache Configuration ---
# Maximum pending messages in session queue
# APP_QUEUE_MAX_SIZE=10

# Maximum KG projects to keep in memory cache (LRU eviction)
# APP_KG_PROJECT_CACHE_MAX_SIZE=100
